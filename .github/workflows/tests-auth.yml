name: tests
on:
  pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup and run tests
        run: |
          ls ${{ github.workspace }}
          node -v
          cd auth
          npm install
          npm run test:ci